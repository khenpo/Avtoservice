version: '3.8'

services:
  backend:
    build: 
      context: .
      dockerfile: backend/Dockerfile
    restart: always
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - ./db:/app/db
      - ./logs:/app/logs
      - ./maps:/app/maps
      - ./backend/prompts:/app/backend/prompts
    depends_on:
      - redis

  redis:
    image: redis:alpine
    restart: always